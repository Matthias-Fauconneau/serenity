CG:
 split editor/lsystem, fix shadows, fix leafs, vertex interpolated lighting, optimize
 Editor: copy-paste, XCompose, syntax highlighting
 Soft Shadows: image space filter, tighter light view scene bounds, check culling
 Trees: improve branches using environment modeling (light and space competition)
 Textures: bark,leafs,twigs
 Scene: sky, terrain, grass, plants, forest...
 Shadow map, LOD
 Hardware rendering: faces, MSAA, raycasted soft shadows (shadow map if still too slow))
 Export .obj [release]
 Software rasterizer: vectorize setup, step grids for vertex attribute interpolations ? coverage sampling ? Hi-Z pixel reject ? float16 colors ?
Desktop: fix links, desktop calculator
IDE: text editor, parser(inherited attributes, scoped symbols, GLR), syntax highlighting, navigation, completion, git
Touchbook: hardware scroll, fix touchscreen, accelerometer trigger, portrait, onscreen keyboard, handwriting recognition
Standards: deflate, jpeg, mp3, vorbis, truetype, ssl
Apps: runner, shell, file manager, search, imap, torrent, video, irc
